# Copy to .env.local if needed. Never commit real secrets.
# Product default: lab densify OFF (D1).

# Research only — do not set on public deploys:
# VITE_LPIN_LAB_SCALE=1

# Optional QA overrides:
# LPIN_BASE_URL=http://127.0.0.1:8090
# LPIN_QA_SHOTS=./screenshots
